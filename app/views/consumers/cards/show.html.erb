<td><h5>登録クレジットカード情報</h5>
			<td><%="**** **** **** " + @default_card_information.last4 %></td>
			<td><%= exp_month = @default_card_information.exp_month.to_s %></td>
			<td><%= exp_year = @default_card_information.exp_year.to_s.slice(2,3) %></td>
			<td><%= exp_month + " / " + exp_year %></td>
			<%= form_for(@card,url:consumers_card_delete_path,method: :post, id: 'charge-form',  name: "inputForm") do |f| %>
			<% f.fidden_field :payjp_id , value: "" %>
			<%= f.submit "削除する" ,class:"btn btn-primary btn-lg center-block" %>
		</td>